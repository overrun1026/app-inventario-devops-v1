# Create a new .env file based on this template.
# Replace the password below with your own PostgreSQL password.
ENV_HOST=localhost
ENV_PORT=5432
ENV_DATABASE=inventario_db
ENV_USER=postgres
ENV_PASSWORD=your_password_here